name: type_master_game
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- Existing Dependencies ---
  flutter_exit_app: ^2.0.0
  stroke_text: any
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8

  # --- NEW DEPENDENCIES (Added to fix errors) ---
  provider: ^6.0.0
  shared_preferences: ^2.2.0
  english_words: ^4.0.0
  url_launcher: ^6.2.0  # Moved from dev_dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/exit.png
    - assets/phrases.txt